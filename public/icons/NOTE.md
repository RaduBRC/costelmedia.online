Generated by `scripts/generate_icons.py` (run `python3 scripts/generate_icons.py`
from the repo root to regenerate) — a violet-600 (`#7c3aed`) rounded square
with a white sparkle, matching the dashboard header's own logo mark. Not
placeholders; this is the real icon set:

- `appointment-192.png` / `appointment-512.png` — standard installability icons (manifest `purpose: "any"`), also what the service worker's `showNotification` calls use.
- `maskable-192.png` / `maskable-512.png` — edge-to-edge background with the sparkle kept inside the safe-zone circle, for Android's adaptive-icon masking (manifest `purpose: "maskable"`).
- `apple-touch-icon.png` — 180×180, flattened (no alpha — iOS renders transparent PNGs as black), referenced directly from `index.html` since iOS Safari doesn't reliably use the manifest's icon list for the home-screen icon.
- `badge-72.png` — simplified violet disc, no sparkle detail (renders too small in a notification tray for it to read anyway).

If the brand mark changes, edit the generator and re-run it rather than
hand-editing these PNGs.
